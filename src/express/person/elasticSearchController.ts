import { Response, Request } from 'express';

export class ElasticPersonController {
    static async searchByFullname(req: Request, res: Response) {
        return 'Hty';
    }
}

export default ElasticPersonController;
